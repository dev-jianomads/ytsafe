// This file is intentionally minimal to avoid runtime errors
// Supabase integration can be added later when properly configured

export const isSupabaseAvailable = () => false;

export const saveSearchToSupabase = async () => {
  // No-op function to prevent errors
  return null;
};